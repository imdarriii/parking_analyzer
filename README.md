# 🅿️ Parking Analyzer

**Система анализа парковочных мест в реальном времени с использованием AI (YOLO)**

Веб-приложение для анализа видеопотока с камер города Бишкека и определения свободных парковочных мест в режиме реального времени.

## 🎯 Проблема и решение

**Проблема:** Водители тратят время на поиск свободных парковочных мест в городе.

**Решение:** Система искусственного интеллекта автоматически анализирует видеопотоки с установленных камер и показывает водителю информацию о свободных местах через веб-приложение.

## 👥 Целевая аудитория

- Водители автомобилей в Бишкеке
- Управления дорожного движения и парковок города
- Операторы и администраторы парковочных зон

## 🛠️ Технологии

| Компонент | Технология |
|-----------|-----------|
| Backend | Python, Flask, Flask-CORS |
| Frontend | HTML5, CSS3, JavaScript |
| AI Модель | YOLOv8 (Ultralytics) |
| Компьютерное зрение | OpenCV |
| Видеопоток | M3U8 Stream (реальная камера Бишкека) |

## 📊 Как это работает

```
Видеопоток камеры
    ↓
YOLOv8 обнаруживает автомобили
    ↓
Система проверяет каждое парковочное место
    ↓
Обновляет статус (ЗАНЯТО/СВОБОДНО)
    ↓
Отправляет данные на фронтенд
    ↓
Пользователь видит свободные места и может забронировать
```

## 🚀 Быстрый старт

### Требования
- Python 3.8+
- pip

### Установка

1. **Клонировать репо:**
```bash
git clone https://github.com/your-username/parking-analyzer.git
cd parking-analyzer
```

2. **Создать виртуальное окружение:**
```bash
python -m venv venv

# Windows
venv\Scripts\activate

# macOS/Linux
source venv/bin/activate
```

3. **Установить зависимости:**
```bash
pip install -r requirements.txt
```

4. **Запустить приложение:**
```bash
python app.py
```

5. **Открыть в браузере:**
```
http://localhost:5000
```

## 📖 Использование

1. **Вход**: Введи своё имя
2. **Запуск анализа**: Нажми кнопку "Запустить анализ"
3. **Просмотр видео**: Смотри трансляцию с камеры в реальном времени
4. **Выбор места**: Нажми на свободное парковочное место
5. **Бронирование**: Место будет зарезервировано за тобой

## 🎥 Исходные данные

Проект анализирует видеопоток с камеры Бишкека:
```
https://cam.kt.kg/cam17/stream.m3u8
```

Координаты парковочных мест настраиваются в коде (файл `app.py`).

## 📁 Структура проекта

```
parking-analyzer/
├── app.py                    # Основной Flask приложение
├── requirements.txt          # Python зависимости
├── templates/
│   └── index.html           # HTML фронтенд с CSS и JS
├── README.md                # Документация
└── .gitignore               # Git ignore файл
```

## 🔌 API Endpoints

### Аутентификация
```
POST /api/auth
Body: { "username": "Иван" }
Response: { "success": true, "message": "...", "user": "Иван" }
```

### Управление анализом
```
POST /api/start    - Запуск анализа
POST /api/stop     - Остановка анализа
GET  /api/health   - Проверка статуса
```

### Получение данных
```
GET /api/status
Response: {
  "is_running": true,
  "free_spots": 7,
  "total_spots": 10,
  "occupancy_rate": 30.0,
  "spots": [...]
}
```

### Видео и бронирование
```
GET  /api/video-feed  - Текущий кадр (base64 JPEG)
POST /api/book        - Бронирование места
Body: { "spot_id": 0, "user": "Иван" }
```

## ⚙️ Конфигурация

### Изменение URL камеры

В файле `app.py` (строка ~15):
```python
self.camera_url = "https://cam.kt.kg/cam17/stream.m3u8"
# Или для локальной камеры:
self.camera_url = 0  # Встроенная веб-камера
# Или видеофайл:
self.camera_url = "path/to/video.mp4"
```

### Координаты парковочных мест

В файле `app.py` (строка ~30):
```python
PARKING_SPOTS = [
    (x1, y1, x2, y2),  # [x_верхнее_левое, y_верхнее_левое, x_нижнее_правое, y_нижнее_правое]
    ...
]
```

## 🐛 Решение проблем

### Ошибка: "Не удалось подключиться к камере"
```bash
# Проверь интернет соединение или попробуй локальный видеофайл
```

### Ошибка: "Модель YOLO не загружена"
```bash
pip install --upgrade ultralytics
```

### Порт 5000 занят
Отредактируй последнюю строку в `app.py`:
```python
app.run(debug=True, host="0.0.0.0", port=8000)
```

## 📊 Производительность

- **FPS**: 10-15 (зависит от железа)
- **Задержка**: ~500ms
- **Использование памяти**: ~200-300 МБ
- **Размер модели**: ~100 МБ

## 📈 Возможные улучшения

- [ ] История бронирований (база данных)
- [ ] Интеграция с GPS картой
- [ ] Уведомления об освобождении мест
- [ ] Оплата через приложение
- [ ] Мобильное приложение (React Native)
- [ ] Поддержка множественных камер
- [ ] Калибровка координат через UI

## 👨‍💼 Команда

- **Разработчик**: [Твоё имя]
- **Университет**: [Твой ВУЗ]
- **Дата**: 2024

## 📄 Лицензия

MIT License - см. файл LICENSE

## 📧 Контакты

- GitHub: [Твой профиль]
- Email: [Твоя почта]

## 🎓 Образовательный проект

Этот проект создан в образовательных целях как демонстрация:
- Работы с компьютерным зрением
- Использования моделей глубокого обучения (YOLO)
- Разработки веб-приложений (Python + HTML/CSS/JS)
- Обработки видеопотоков в реальном времени

---

**Статус**: ✅ Готово к использованию

**Последнее обновление**: 2024