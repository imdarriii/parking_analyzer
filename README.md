# 🅿️ Parking Analyzer

**Система анализа парковочных мест в реальном времени с использованием AI (YOLO)**

⚠️ **ДЕМО-ВЕРСИЯ** | Веб-приложение для анализа видеопотока с камер Бишкека, которое помогает водителям быстро и удобно находить свободные парковочные места благодаря искусственному интеллекту.

## 🎯 Основная идея

Поиск парковочного места — реальная проблема для водителей города. По результатам наших опросов, **72,9% водителей** считают это серьезной проблемой, испытывая стресс и нервозность во время поиска. При этом **92,7% опрошенных** заявили, что приложение для поиска свободных мест было бы для них удобным и комфортным.

**Наше решение**: веб-сервис, который использует искусственный интеллект (YOLOv8) для анализа видеопотоков с городских камер в режиме реального времени. Система автоматически определяет свободные и занятые парковочные места и выводит эту информацию пользователю.

**Результат**: водители экономят время, снижают стресс и помогают облегчить городские пробки.

## 📊 Текущий статус

Это **демо-версия** проекта, которая использует прямую трансляцию с общественной камеры на площади Ала-Тоо в Бишкеке. 

**Почему демо?** Камера показывает всю городскую площадь, а не только парковку, что создает сложности для анализа:
- Ракурс далёкий и размер машин очень маленький
- Кадр насыщен людьми, машинами и другими объектами
- Распознавание парковочных мест нередко сложно даже для человеческого глаза

Несмотря на эти ограничения, модель обучена распознавать парковочные места даже в таких сложных условиях.

## 🚀 Попробовать сейчас

### Локально

1. **Клонировать репозиторий:**
```bash
git clone https://github.com/imdarriii/parking_analyzer.git
cd parking_analyzer
```

2. **Создать виртуальное окружение:**
```bash
python -m venv venv

# Windows
venv\Scripts\activate

# macOS/Linux
source venv/bin/activate
```

3. **Установить зависимости:**
```bash
pip install -r requirements.txt
```

4. **Запустить приложение:**
```bash
python app.py
```

5. **Открыть в браузере:**
```
http://127.0.0.1:5000
```

## 👥 Целевая аудитория

- Водители автомобилей в Бишкеке, ищущие свободные парковочные места
- Управления дорожного движения и транспорта города
- Организации и здания с собственными парковками и камерами видеонаблюдения

## 🛠️ Технологии

| Компонент | Технология |
|-----------|-----------|
| Backend | Python, Flask, Flask-CORS |
| Frontend | HTML5, CSS3, JavaScript |
| AI Модель | YOLOv8 (Ultralytics) |
| Компьютерное зрение | OpenCV |
| Видеопоток | M3U8 Stream (камера площади Ала-Тоо, Бишкек) |

## 📖 Как пользоваться

1. **Регистрация**: Введи своё имя в поле при входе
2. **Запуск анализа**: Нажми кнопку "Запустить анализ"
3. **Просмотр видео**: Смотри прямую трансляцию с камеры в реальном времени
4. **Анализ мест**: Видишь, как AI анализирует парковочные места
5. **Выбор места**: Легко определяешь, какие места свободны (зелёные), какие заняты (красные)
6. **Экономия времени**: Быстро принимаешь решение о парковке

## 🎥 Источник видео (текущая демо-версия)

Проект анализирует видеопоток с общественной камеры на площади Ала-Тоо:
```
https://cam.kt.kg/cam17/stream.m3u8
```

## 📊 Как это работает

```
1️⃣ Видеопоток с камеры
    ↓
2️⃣ YOLOv8 анализирует кадр и обнаруживает автомобили
    ↓
3️⃣ Система проверяет каждое парковочное место
    ↓
4️⃣ Определяет статус: ЗАНЯТО или СВОБОДНО
    ↓
5️⃣ Отправляет результаты на фронтенд
    ↓
6️⃣ Пользователь видит визуализацию свободных и занятых мест
```

## 📁 Структура проекта

```
parking-analyzer/
├── app.py                    # Основное Flask приложение с AI моделью
├── requirements.txt          # Зависимости Python
├── templates/
│   └── index.html           # Веб-интерфейс (HTML, CSS, JavaScript)
├── README.md                # Документация
└── .gitignore               # Git ignore файл
```

## 🔌 API Endpoints

### Аутентификация пользователя
```
POST /api/auth
Body: { "username": "Иван" }
Response: { "success": true, "message": "...", "user": "Иван" }
```

### Управление анализом
```
POST /api/start    - Запуск анализа видео
POST /api/stop     - Остановка анализа
GET  /api/health   - Проверка статуса системы
```

### Получение информации о парковочных местах
```
GET /api/status
Response: {
  "is_running": true,
  "free_spots": 7,
  "total_spots": 10,
  "occupancy_rate": 30.0,
  "spots": [...]
}
```

### Видеопоток
```
GET /api/video-feed  - Текущий кадр с анализом (base64 JPEG)
```

## 🚀 Планы развития проекта

### Этап 2: Улучшение точности
В будущем планируется работа не только с открытыми онлайн-камерами, но и сотрудничество с:
- Приватными зданиями и организациями
- Торговыми центрами
- Офисными комплексами
- Любыми структурами, имеющими камеры видеонаблюдения, направленные на парковки

**Преимущества**: Камеры будут фокусироваться только на парковочной зоне, что позволит:
- Значительно улучшить точность анализа
- Стабильнее определять свободные места
- Обучить модель на качественных данных
- Масштабировать сервис на весь город

### Итоговая цель
Создать полноценное решение для города, которое:
- Помогает водителям экономить время и снижать стресс
- Уменьшает городские пробки из-за поиска парковки
- Оптимизирует использование существующих парковочных мест

## ⚙️ Конфигурация

### Изменение источника видео

В файле `app.py`:
```python
self.camera_url = "https://cam.kt.kg/cam17/stream.m3u8"
# Или для встроенной веб-камеры:
self.camera_url = 0
# Или для видеофайла:
self.camera_url = "path/to/video.mp4"
```

### Определение парковочных мест

В файле `app.py`:
```python
PARKING_SPOTS = [
    (x1, y1, x2, y2),  # координаты: верхний_левый_x, верхний_левый_y, нижний_правый_x, нижний_правый_y
    ...
]
```

## 🐛 Решение проблем

### Ошибка: "Не удалось подключиться к камере"
Проверь интернет-соединение или используй локальный видеофайл вместо онлайн-потока.

### Ошибка: "Модель YOLO не загружена"
```bash
pip install --upgrade ultralytics
```

### Порт 5000 уже занят
Отредактируй последнюю строку в `app.py`:
```python
app.run(debug=True, host="0.0.0.0", port=8000)
```

## 📊 Характеристики производительности

- **Частота кадров**: 10-15 FPS
- **Задержка анализа**: ~500ms
- **Использование памяти**: ~200-300 МБ
- **Размер модели YOLOv8**: ~100 МБ

## 🔗 Ссылки

- **GitHub репозиторий**: https://github.com/imdarriii/parking_analyzer.git
- **Локальный адрес**: http://127.0.0.1:5000 (после запуска `python app.py`)

## 🎓 Образовательный проект

Проект демонстрирует:
- Работу с компьютерным зрением (OpenCV)
- Использование моделей глубокого обучения (YOLOv8)
- Разработку веб-приложений на Python (Flask)
- Обработку видеопотоков в реальном времени
- Практическое применение AI для решения городских проблем

---

**Статус**: ✅ Демо-версия готова к использованию

**Главная цель**: Показать, что проект уже работает и приносит реальную пользу. С доступом к большему количеству качественных камер модель будет обучаться лучше и стать полноценным решением для города.